#include <bits/stdc++.h>
using i64 = long long;
#ifdef LOCAL
#include "debugutil.h"
#else
#define debug(...) 666
#define debugArr(arr, n) 666
#endif

class Solve {
public:
    void solve() {
        return;
    }
};

int32_t main() {
    std::cin.tie(0)->sync_with_stdio(0);
    std::cin.exceptions(std::cin.failbit);
    int tt = 1;
    std::cin >> tt;
    while (tt--) {
        Solve solver;
        solver.solve();
    }
}
